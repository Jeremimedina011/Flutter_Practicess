import 'package:flutter/cupertino.dart';

class InformatioD extends StatelessWidget {
  const InformatioD({super.key});

  @override
  Widget build(BuildContext context) {
    return const Padding(
      padding: EdgeInsets.all(32),
      child :Text(
        'By-  JEREMI ALONZO MEDINA MEDINA - 5B - DSM',
        
      ), 
    );
  
  }
}
